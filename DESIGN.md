# Visual Design System (DESIGN.md)

本ファイルは、AIエージェントが「焼き鳥・居酒屋」業態のUIコード（HTML, CSS, React, など）を生成する際に参照するデザイン仕様書です。UIを構築する際は、必ずこのルールに従ってください。

※本仕様は、提供いただいた参考サイト（新潟の飲食グループ「Tsubamesanjo Bit」および神戸牛専門店「辰屋」のスクリーンショット）を分析し、焼き鳥・居酒屋（例：「炭火焼鳥 ○○」）向けに再構成したものです。カラーコードは画像からの推定値のため、実装時に微調整してください。

## 1. トーン＆マナー
* **ブランドイメージ:** 炭火と煙の熱量、大人の隠れ家的高級感、和モダン、職人の手仕事へのこだわり
* **コアバリュー:** 炭火が爆ぜる「食欲をそそる躍動感」と、老舗割烹のような「静謐で上質な信頼感」の両立
* **世界観:** 漆黒の背景に浮かぶ炎・炭火のオレンジ、木目・木炭のようなテクスチャ、大型の欧文セリフロゴと縦書きの和文タイポグラフィを効果的に使い、単なる「居酒屋サイト」ではなく「隠れ家的な目的地」のような特別感を演出する

## 2. カラーパレット (Colors)
指定された役割（トークン）に応じて、以下のカラーコードを厳密に使用してください。
* **Primary（ブランドカラー／深紅・熾火色）:** `#6E2A33` (例: 熾火や紅葉を思わせる深い赤ワイン色。見出しアクセント、フッター、CTAの縁取りに使用)
* **Primary Dark（ホバー・最も暗い赤黒）:** `#4A1B22`
* **Accent（炭火の炎色／琥珀オレンジ）:** `#C1631F` (炎の火花・グラデーションに使用する暖色アクセント。CTAボタンや価格表記など「強い訴求」に使用)
* **Accent Dark（ホバー用）:** `#A34F17`
* **Background（メインダーク背景／漆黒）:** `#17130F` (例: 炭・鉄板のような黒。ヒーロー画像やフッターの下地に使用)
* **Background Light（セクション背景／生成り）:** `#F5E4D8` (和紙・障子紙のような温かいオフベージュ。ヘッダーや明るいセクションの下地)
* **Surface（カード背景）:** `#FFFFFF` または `#FDFBF6` (メニューカードなど、写真・料理を引き立てる面)
* **Text Main（本文・見出しテキスト／明背景用）:** `#231F1B` (黒に近いチャコールブラウン)
* **Text Light（本文・見出しテキスト／暗背景用）:** `#F5E4D8` (漆黒・深紅背景の上に乗せる生成りテキスト)
* **Text Muted（薄いテキスト／日付・キャプション）:** `#9C8F82`
* **Border（境界線・区切り線／明背景）:** `#E3D2BC`
* **Border Dark（境界線・区切り線／暗背景）:** `#3A2E28`
* **Accent Tag（特長ラベル背景）:** `#6E2A33`（生成り文字との組み合わせで使用）
* **Section Accent（こだわり・ストーリーページ背景／淡い藍鼠）:** `#AEBEC2` (店主紹介・ブランドストーリーなど、写真1枚を主役にしたページでのみ使用する差し色。多用しない)

## 3. タイポグラフィ (Typography)
* **フォントファミリー（日本語・見出し）:** `"Shippori Mincho", "Noto Serif JP", "Hiragino Mincho ProN", serif`（明朝体で、炭火焼きの職人らしさ・老舗感を表現）
* **フォントファミリー（日本語・本文）:** `"Noto Serif JP", "Hiragino Mincho ProN", serif`（見出しと統一し、上品な世界観を保つ）
* **フォントファミリー（英数字・ロゴ／装飾文字）:** `"Cormorant Garamond", "EB Garamond", serif`（大型の欧文ロゴ（例:店名の英字表記）や"View More"などの装飾リンクに使用する、細身でエレガントなセリフ体）
* **ベースフォントサイズ:** `16px` (1rem)
* **見出し 1 (h1):** `3rem` (48px)〜画面幅に応じて可変, `line-height: 1.3`, 通常ウェイト、ヒーロー画像に重ねる大型ステートメント見出し向け（例: 店名の英字ロゴタイプ）
* **見出し 2 (h2):** `1.75rem` (28px), `line-height: 1.5`, 通常
* **本文 (p):** `1rem` (16px), `line-height: 1.9`（和文は行間を広めに取り、可読性を担保）
* **ナビゲーション・ラベル文字:** `0.875rem` (14px), 文字間隔（letter-spacing）を `0.1em` 程度広めに取る
* **縦書き（writing-mode: vertical-rl）:** 「営業日のご案内」のような画面端の補助ラベルに使用可（メインナビゲーションは横書きの上部固定バーを基本とする）

## 4. スペースとレイアウト (Spacing)
余白や配置は、以下の8pxグリッドシステム（ベースは4px）を基準に設定してください。全体的にゆったりとした余白を取り、高級感・非日常感を演出します。
* `px` (Padding/Margin): `4px`
* `spacing-1` (最小の余白): `8px`
* `spacing-2` (標準的な余白): `16px`
* `spacing-3` (セクション内要素間): `32px`
* `spacing-4` (セクション間の余白): `80px`
* `spacing-5` (ヒーローセクションなど大余白): `120px`
* **レイアウト方式:** CSS Grid を優先。フルブリードの写真（炭火・炎・食材のクローズアップ）を主役に据え、その上に大型欧文ロゴと短いキャッチコピーを重ねる非対称レイアウトを基本構成とする。
* **ヘッダー構成:** 画面上部に固定の横並びナビゲーションバー（生成り背景）を配置し、中央にロゴマーク、右端に「ご予約」等のCTAリンクまたはカートアイコンを配置する構成を標準とする。
* **パンくずリスト:** 下層ページ（店舗詳細・事業紹介など）のヒーロー直下に「ホーム ── 店舗情報 ── 個別ページ名」のように `──`（ダッシュ）区切りで配置し、リンク項目には下線を付ける。文字サイズは `0.875rem`、`Text Muted` または `Text Main` を使用。
* **下層ページのダーク全幅セクション:** 店舗詳細・お知らせ詳細などの本文ページでは、ヒーロー以降の本文エリア全体を `Background`（漆黒）背景＋`Text Light` 文字色で構成し、生成り背景のヘッダーとのコントラストで「特別な情報」であることを印象づける。

## 5. コンポーネントスタイル (Components)
* **ボタン（プライマリCTA／View More等の遷移リンク）:**
  * 形状: テキスト＋右側に円形の矢印アイコンを添えた横長リンク型
  * 背景: 透明、円形アイコンのみ `Text Light` または `Primary` の縁取り＋塗り
  * 文字色: 背景に応じて `Text Light` または `Text Main`、英字セリフ体を使用
  * 角丸 (border-radius): アイコン部分は `9999px`（完全な円）
  * ホバー時のエフェクト: 円形アイコンの背景色を反転、または `transform: translateX(4px)`
* **ボタン（通常／予約・購入など）:**
  * 角丸 (border-radius): `24px`（ピル型）
  * パディング: 縦 `12px`, 横 `24px`
  * 背景色: `Accent` (`#C1631F`)、文字色 `#FFFFFF`
  * シャドウ: `none`
  * ホバー時のエフェクト: 背景色を `Accent Dark` に変化し、`transform: translateY(-2px)` を適用
* **カード（メニュー・商品カード）:**
  * 背景: `Surface` (`#FFFFFF` / `#FDFBF6`)
  * 画像: カード上部いっぱいに配置、角丸 `12px`（上2角のみ、または全体）
  * 角丸 (border-radius): `12px`
  * 境界線: なし、または `1px solid [Border Color]`
  * 影 (box-shadow): `none`（フラットで上品な印象を優先）
  * 価格表記: `Accent Color` の太字で強調
* **特長タグ（ラベル）:**
  * 背景: `Primary` (`#6E2A33`)、文字色 `#F5E4D8`
  * 形状: 短冊型の小さい矩形バッジ、見出しの脇に添える
* **予約カレンダーウィジェット:**
  * 背景: `Background`（漆黒）または `#232019` のダークグレー
  * 当日セル: `Primary` 色の円で囲む
  * 定休日・休業日: 斜線パターン（ハッチング）で表現
* **背景装飾:**
  * ヒーローセクション下部や暗色セクションに、薄い木目・木炭のグレイン（ノイズテクスチャ）を重ね、和紙のような質感を演出
  * 炎・火花をイメージしたオレンジ〜赤のグラデーションを、写真の一部やセクション境界にさりげなく使用
* **ヘッダー（PC／固定バー）:**
  * 画面上部固定、`Background Light`（生成り）背景
  * 左: メニューアイコン、中央: ロゴマーク（英字イニシャル等）、右: ナビゲーション項目＋カート/予約アイコン
  * 角丸なし、影なし（フラットに画面上端へ密着）、スクロール時は半透明の暗色オーバーレイに切り替え可
* **フッター:**
  * `Primary`（深紅）または `Background`（漆黒）の全幅背景ブロック
  * サイトマップ・店舗一覧・SNSリンクを3〜4カラムに分割配置、文字色は `Text Light`
* **下部固定予約バー（PC／全ページ共通）:**
  * 画面最下部に `Background`（漆黒）の全幅バーを固定表示
  * 左: 「ご予約・お問い合わせ」等のラベル（`Text Light`）
  * 中央〜右: 「営業日カレンダー」（アイコン＋テキスト、アウトライン/白文字）、電話番号（受話器アイコン＋テキスト）を横並びに配置
  * 右端: `Primary` 背景のピル型CTAボタン（例:「空席確認・ご予約」＋外部リンク矢印アイコン、`Text Light`）
* **数字付き一覧（事業紹介・カテゴリ一覧など）:**
  * 写真＋タイトル（英字セリフ体・下線付き）＋右上に通し番号（`01` `1` 等、細身のセリフ体）＋和文サブタイトルの組み合わせを1ユニットとする
  * 3〜4カラムのグリッドで横並びに配置し、写真は正方形〜横長でトリミング
* **カテゴリサイドタブ（商品一覧の絞り込み）:**
  * 画面左側に縦積みのテキストリンクリストを配置し、各項目の左に小さな円形ドットアイコン（二重丸）を添える
  * 選択中の項目は `Primary` 色のドット＋太字、非選択は `Text Muted`
  * リスト最下部に「一覧」等の全件表示リンクを縦書きで添えてもよい
* **評価スター表示:**
  * 商品名・価格の直下に、塗りつぶし星アイコン5つ＋数値（例: `4.90`）＋`/ 421件` のレビュー件数をワンラインで表示
  * 星の色は `Accent`、件数テキストは `Text Muted`
* **価格表記（2段組）:**
  * 1行目: 数量・人数分＋価格を太字で強調（例:「2-3人前（400g）6,912円〜」）
  * 2行目: 単価の補足情報を `Text Muted` の小さめの文字で添える（例:「100gあたり1,728円」）
* **マスコットキャラクター＋吹き出し:**
  * 画面左下に `position: fixed` で固定配置し、スクロール位置に関わらず常時表示させる
  * 隣接する吹き出し（白背景・黒枠、または短冊型の縦書きラベル）で一言コメントやチャット導線を添える
  * 硬質になりがちな高級路線のトーンに、親しみやすさを一点加える差し色的な使い方に留める
* **ブランドストーリーの写真オーバーレイ見出し:**
  * 1枚の大きな写真の上に、細い白枠（`1px solid #FFFFFF`）で囲んだ矩形ボックスを重ね、その中に短い日本語見出し（例:「〇〇について」）を明朝体で配置する
  * 周辺に店主・職人のイラストや手描き素材を添えて、写真の説明的な雰囲気を和らげる
* **ハンバーガーメニュー（全画面オーバーレイ／モバイル）:**
  * ハンバーガーアイコンタップで `Surface`（白）または `Background Light`（生成り）の全画面オーバーレイを表示し、右上に閉じる用の `×` アイコンを配置
  * 「ログイン／新規登録」等のアカウント関連リンクを上部に、カテゴリリンクは見出し＋2カラムグリッド（各項目に小さな丸矢印アイコンを添える）で整理し、セクションごとに細い罫線で区切る
  * 画面下部にはマスコットキャラクター＋吹き出し（チャット導線）を重ねて表示してもよい
* **絞り込みタブ（商品一覧の表示切り替え）:**
  * 「種類で」「価格・人数で」のように2〜3個の見出しをタブ状に横並びし、選択中のタブの下にのみ `Primary` 色の下線を表示する
  * タブ下段には、丸矢印アイコン付きの横スクロール可能なカテゴリチップ（すき焼き肉／ステーキ／しゃぶしゃぶ肉…）を並べる
* **縦書きメッセージリボン（差し色の帯ラベル）:**
  * ダーク背景のフッター・店舗情報エリアの左端などに、`Section Accent`（淡い藍鼠・水色系）の縦長の帯を配置し、内側に短い一言メッセージを縦書きで表示する
  * ページの重要な差し色として一箇所程度に絞って使用し、多用しない

## 6. レスポンシブ振る舞い (Responsive)
* 参考サイトはPC・モバイルともに上部固定の横型ヘッダーを採用しているため、**モバイルファースト**を基本としつつ、PCでは余白とフォントサイズを拡張する形で設計する。
* **ブレイクポイント:**
  * スマホ (Mobile): `~ 767px`
  * タブレット (Tablet): `768px ~ 1023px`
  * デスクトップ (Desktop): `1024px ~`
* **モバイルヘッダー:**
  * 画面上部に `sticky`/`fixed` で固定、`Background Light`（生成り）または `Surface`（白）背景
  * 左: カレンダー/リスト等の補助アイコン、中央〜左寄り: ロゴマーク（縮小版）、右: カートアイコン＋ハンバーガーアイコン（3本線）を横並びに配置する
  * 予約用のCTAボタンはヘッダー内には常設せず、下部固定予約バー、またはハンバーガーメニュー内の導線に集約する
  * スクロールして暗色セクションに入ると、ヘッダー背景を `Primary` の半透明などに切り替えてもよい
* **ヒーローセクション:**
  * PCでは横並び2分割の写真（例: 料理クローズアップ＋炭火の炎）、モバイルでは縦積み1カラムに変更し、写真の高さを画面幅基準で調整
  * 大型英字ロゴタイプは、モバイルでは折り返しを許容しつつ中央揃えを維持する
* **縦書き・大型装飾テキストの扱い:**
  * PCの `writing-mode: vertical-rl` による端の補助ラベル（「営業日のご案内」等）は、モバイルではハンバーガーメニュー内、または非表示に統合する
* **カード型メニュー一覧:**
  * PCでは2〜3カラムグリッド、モバイルでは1カラム表示に変更し、画像はカード上部いっぱい（幅100%）、角丸は上2角のみ、価格・説明文はその下に縦積み
* **フッター:**
  * PCでは3〜4カラムの横並び、モバイルでは折りたたまずに「site map」「restaurant/shop」「connect with us」等の見出しごと縦に積み、各見出し配下のリンクをそのまま全件表示する（アコーディオン化は必須としない）
* **ハンバーガーメニュー:**
  * モバイル・タブレットでは全画面オーバーレイのメニューを基本とし、PCでは画面上部の横並びナビゲーションに戻す
* **カテゴリタブ・チップ（絞り込みタブ／サイドタブ共通）:**
  * PCの「絞り込みタブ＋カテゴリサイドタブ」は、モバイルではどちらも横スクロール可能なチップ（タグ）リストに統一し、ドットアイコンは省略可
* **下部固定予約バー:**
  * PCでは「ラベル＋カレンダー＋電話番号＋CTAボタン」を横一列で表示、モバイルでは電話番号・カレンダーを省略し、右端の `Primary` ピルCTAボタンのみを画面幅いっぱいに近い形で残す
* **パンくずリスト:**
  * モバイルでは階層をすべて表示すると横幅を圧迫するため、直前の階層のみ「＜ 店舗情報」のように簡略表示してもよい
* **数字付き一覧（事業紹介など）:**
  * PCでは3〜4カラムグリッド、モバイルでは1カラムで縦積みにし、通し番号は写真右上に小さく残す

## 7. アクセシビリティ (Accessibility)
* すべてのテキスト（特に価格・注意書き・エラーメッセージ）は、背景色とのコントラスト比を `4.5:1` 以上にすること。特に `Background`（漆黒 `#17130F`）上の `Text Muted`（`#9C8F82`）は視認性が低くなりやすいため、本文には使用せずキャプション用途に限定する。
* 暗色背景（`Primary` / `Background`）上のテキストは、原則 `Text Light`（`#F5E4D8`）を使用し、`Accent`（`#C1631F`）を地の文の文字色として使用しない（装飾・アイコンにとどめる）。
* 画像には必ず `alt` 属性を記述すること。
* 縦書きテキストを含む要素は、スクリーンリーダー利用時の読み上げ順序に配慮し、視覚的な配置順とDOM順序を一致させること。

## 8. エージェントプロンプトガイド (Agent Prompt)
AIエージェントへの指示です。コードを生成する際は、以下のルールを遵守してください。
1. 上記のトークン（変数名またはカラーコード）をハードコードせず、可能な限りCSS変数またはTailwindのクラスを用いて表現してください。
2. 明朝体（serif）を基調とし、ゴシック体（sans-serif）は極力使用しないこと。和文の品格・老舗感を損なわないようにする。
3. 「炭火」「炎」「木目」を想起させる暖色（`Accent`）と、漆黒（`Background`）・深紅（`Primary`）のコントラストを積極的に使い、単調な配色にならないようにする。
4. 写真・料理・炭火の炎が主役となるレイアウトを心がけ、テキストや装飾で写真の視認性を妨げないこと。
5. 未知のデザイン要素を生成する場合は、事前にユーザーに質問するか、このデザインシステムに準拠した形で提案してください。
6. マスコットイラストや吹き出しなどの「親しみ要素」は、下層ページ（商品一覧・店舗詳細など実務的なページ）にのみ控えめに使用し、トップページやブランドストーリーの高級感あるトーンを壊さないようにすること。
