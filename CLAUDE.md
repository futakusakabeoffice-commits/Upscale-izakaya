# CLAUDE.md

このファイルは、本リポジトリで作業する Claude Code に向けたプロジェクト概要です。

## プロジェクト概要

「炭火焼鳥 燈（あかり）」という**架空の**炭火焼鳥専門店のコーポレートサイト（トップページ）。
Claudeデザイン経由で作成された静的サイトで、現状は素の HTML / CSS / JS のみで構成されている（ビルドツール・フレームワーク・パッケージマネージャは未導入）。

※実在の店舗・団体とは無関係のサンプルサイトです。

## ソース・オブ・トゥルース（正）

このプロジェクトには、コンテンツとデザインの仕様書がリポジトリ直下に置かれている。**新しいコピーやスタイルを追加・変更する際は、まずこの2ファイルを確認すること。**

- **`CONTENTS.md`** — テキスト・構成の正。見出し文言、本文、メニュー内容、店舗情報、フッターのリンクなどはすべてここに準拠する。
- **`DESIGN.md`** — 見た目（ビジュアルデザインシステム）の正。カラートークン、フォント、スペーシング、コンポーネント仕様、レスポンシブ仕様、アクセシビリティ基準を定義している。

新規のUI要素を作る場合は、まず `DESIGN.md` の該当ルールを探し、それに準拠させる。ルールに無い要素は、DESIGN.md のトーン＆マナー（炭火・煙・和モダン・隠れ家的高級感）から逸脱しないように独自に判断するか、ユーザーに確認する。

## ファイル構成

```
index.html       トップページ本体（唯一の実装済みページ）
css/styles.css    デザインシステムをCSSカスタムプロパティ（:root）で実装
js/script.js      モバイルハンバーガーメニューの開閉制御のみ
images/           画像プレースホルダ用ディレクトリ（現状は空。<img>のsrcはダミーパス）
CONTENTS.md       コンテンツ仕様（正）
DESIGN.md         デザイン仕様（正）
```

## 実装状況・既知の注意点

- **トップページのみ実装済み。** グローバルナビ／フッターは `こだわり.html`・`メニュー.html`・`店舗情報.html`・`お知らせ.html`・`ご予約.html` にリンクしているが、これらのページ自体はまだ存在しない。下層ページを作る際は `DESIGN.md` の「下層ページのダーク全幅セクション」「パンくずリスト」仕様に従うこと。
- `CONTENTS.md` の「6. メディア（#media）」セクション（掲載情報／メディア掲載実績）は `index.html` に未実装。
- `images/` 配下の画像は未用意（すべてプレースホルダ）。実画像を追加する際は `alt` 属性を `CONTENTS.md` の指定文言に合わせる。
- 各種電話番号・住所は `00-0000-0000` / `〇〇県〇〇市〇〇町` 等のダミー値（サンプルサイトのため意図的な仮値）。

## デザイントークン早見表（詳細は DESIGN.md）

| 役割 | 変数名 (CSS) | 値 |
| --- | --- | --- |
| Primary（深紅） | `--primary` | `#6E2A33` |
| Primary Dark | `--primary-dark` | `#4A1B22` |
| Accent（琥珀オレンジ） | `--accent` | `#C1631F` |
| Accent Dark | `--accent-dark` | `#A34F17` |
| Background（漆黒） | `--bg` | `#17130F` |
| Background Light（生成り） | `--bg-light` | `#F5E4D8` |
| Surface | `--surface` | `#FDFBF6` |
| Text Main | `--text-main` | `#231F1B` |
| Text Light | `--text-light` | `#F5E4D8` |
| Text Muted | `--text-muted` | `#9C8F82` |

フォント: 見出し・本文とも明朝体（`Shippori Mincho` / `Noto Serif JP`）。欧文ロゴ・装飾のみ `Cormorant Garamond`。ゴシック体は使用しない。

## 作業時の原則

1. 色・フォント・余白はハードコードせず、`css/styles.css` の CSS カスタムプロパティを使う。
2. 明朝体を基調とし、ゴシック体は極力使わない。
3. 写真・料理・炭火が主役。テキストや装飾で視認性を妨げない。
4. 暗背景（Primary/Background）上の本文テキストは `Text Light` を使用し、`Accent` を地の文の文字色にしない（装飾・アイコンのみ）。`Background` 上の `Text Muted` は本文に使わずキャプション用途限定。
5. 画像には必ず `alt` 属性を付ける。
6. マスコット等の「親しみ要素」はトップページには使わず、下層の実務ページ向けに留める（DESIGN.md 8-6）。
7. 未知のデザイン要素を追加する場合は、DESIGN.md に準拠する形で提案するか、ユーザーに確認する。
